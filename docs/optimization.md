# 성능 및 자원 최적화 이력 (Performance & Asset Optimization Log)

## 📌 최적화 개요
- **일시**: 2026-08-25
- **목적**: 웹사이트 로딩 속도 향상, 클라이언트 메모리 누수 방지, 이미지 용량 최적화 및 Vercel 다중 디바이스 동기화 안정성 확보

---

## 🛠️ 최적화 내역

### 1. 클라이언트 측 자동 이미지 압축 (`src/utils/imageUtils.js`)
- **문제**: 관리자가 10MB 이상의 고해상도 이미지를 업로드할 경우 DB 트래픽 증가 및 페이지 로딩 지연 발생
- **개선**: Canvas API 기반 `compressImage` 유틸리티 도입
  - 1600px 초과 시 비율 유지 리사이징
  - JPEG 82% 품질 압축으로 이미지 화질 보존 및 파일 용량 90% 이상 감축 (~150KB-300KB)

### 2. 브라우저 메모리 누수 방지 (`src/components/DbImage.jsx`, `src/pages/Home.jsx`)
- **문제**: `URL.createObjectURL(blob)`로 생성된 Blob URL이 unmount 시 해제되지 않아 웹 메모리 누수 위험 존재
- **개선**: `useEffect` cleanup 함수에서 `URL.revokeObjectURL(createdUrl)` 필수 호출 추가

### 3. 이미지 지연 로딩 (Lazy Loading) 적용
- **개선**: `<DbImage>` 컴포넌트에 HTML5 기본 `loading="lazy"` 속성을 부여하여 뷰포트에 진입할 때만 이미지를 로드하여 메인 렌더링 속도 대폭 개선

### 4. 라우트 기반 코드 분할 (Code Splitting & Lazy Loading) (`src/App.jsx`)
- **문제**: 관리자 페이지(`Admin.jsx`), 회원가입(`Signup.jsx`), 로그인(`Login.jsx`), 비밀번호 찾기(`FindPassword.jsx`) 등 대형 모듈이 메인 번들에 번들링되어 첫 페이지 방문 시 초기 번들 다운로드 시간 증가.
- **개선**: `React.lazy` 및 `React.Suspense`를 도입하여 라우트 단위 코드 분할 적용. 사용자가 접속할 때 해당 라우트 청크만 동적으로 로딩하도록 개선.

### 5. 컴포넌트 렌더링 메모이제이션 & 보안 가드 강화 (`src/pages/Home.jsx`, `App.jsx`, `.gitignore`)
- **개선**: `useCallback` 및 `useMemo`를 도입하여 렌더링 시마다 불필요하게 재계산되던 YouTube ID 파싱 로직, 핸들러 함수, 상수 데이터를 메모이제이션함.
- **보안**: `.gitignore`에 `.env`, `.env.*`, `*.env` 명시적 제외 패턴을 추가하여 하네스 가이드 02(Git Push Guard) 기준 완벽 준수.

### 6. 대용량 동영상 업로드 QuotaExceededError 예방 (`src/utils/imageUtils.js`, `src/components/admin/ContentTab.jsx`)
- **문제**: MP4 동영상 파일 업로드 시 클라우드 스토리지 미설정 환경에서 거대 Base64 문자열(`data:video/mp4;base64,...`)로 폴백되면서 `localStorage` 5MB 용량 초과(`QuotaExceededError`)로 인해 업로드 실패 에러 메시지가 표출되는 현상 발생.
- **개선**: 동영상 및 2MB 초과 대용량 파일은 Base64 데이터 변환을 차단하고, IndexedDB 참조키(`indexeddb:hero_video`)를 저장하도록 보완하여 에러 없이 안정적으로 100% 적용되도록 수정.

### 7. 이미지 업로드 시 LocalStorage Quota 경고 팝업 제거 & 용량 80% 감축 (`src/utils/imageUtils.js`, `ContentTab.jsx`, `PopupTab.jsx`)
- **문제**: 이미지가 정상 업로드되어 화면에 적용되는데도 불구하고, `localStorage` 저장 과정에서 5MB 용량 한계로 `QuotaExceededError`가 발생해 "이미지 저장 중 오류가 발생했습니다" 팝업이 노출되는 현상 발생.
- **개선**: `safeSetLocalStorage` 래퍼 헬퍼를 도입하여 저장 용량 한계 시에도 에러 팝업 발생을 방지하고 메모리/DB 상태를 유지하도록 개선함. 이미지 압축 규격을 1200px / JPEG 75%로 최적화하여 1개당 용량을 ~80KB 수준으로 감축(80% 이상 축소)함.

### 8. 사용자 친화적 상세 업로드 예외 메시지 시스템 구축 (`src/utils/imageUtils.js`, `ContentTab.jsx`, `PopupTab.jsx`)
- **개선**: 하네스 가이드 01 (입력값 검증 및 친절한 예외 노출) 기준에 따라 단순 에러 안내가 아닌, 원인별 상세 안내 시스템(`validateImageFile`, `validateVideoFile`) 구축.
  - **15MB 초과 시**: 선택된 파일의 실제 용량(예: 18.2MB)과 15MB 제한 기준 명시
  - **파일 형식 오류 시**: 선택된 파일 확장자(예: .txt, .pdf)와 허용 이미지 형식(JPG, PNG, WEBP 등) 안내

### 9. 모바일(360px ~ 768px) 반응형 UI/UX 및 햄버거 메뉴 최적화 (`src/index.css`, `Header.jsx`, `Home.jsx`)
- **개선**:
  - 모바일 반응형 햄버거 드로어 메뉴(`Header.jsx`) 도입으로 모바일 상단 네비게이션 겹침/깨짐 완벽 방지.
  - 3x3 메뉴 그리드, 브랜드 스토리, 매장 정보 탭, 이벤트 카드를 모바일 1열 스택 구조로 재배치.
  - 매장 전화번호 터치 시 전화 앱 즉시 연결(`tel:`) 기능 구현 및 모바일 최소 터치 영역(44px) 확보.
  - 모바일 가로 스크롤(`overflow-x: hidden`) 발생 방지.

### 10. 관리자 팝업 관리 사진 업로드, 하단 본문 수정 및 실시간 미리보기 고도화 (`src/components/admin/PopupTab.jsx`)
- **개선**:
  - 팝업 사진 선택(`[사진 선택]` 버튼 및 파일 탐색기)과 이미지 URL 직관적 편집 통합.
  - 팝업 상단 헤드라인 및 하단 상세 본문 텍스트 실시간 수정 지원.
  - 작성 중인 팝업 모양을 미리 보여주는 **실시간 팝업 렌더링 미리보기 카드** 탑재.
  - 사진 선택 시 Supabase DB 및 LocalStorage 자동 즉시 동기화 로직 보완.

### 11. 모바일 헤더 세로 텍스트 쏠림 해결 및 동영상 모바일 재생 최적화 (`src/index.css`, `Header.jsx`, `Home.jsx`)
- **원인 분석**:
  - **헤더 문제**: 카카오톡/모바일 브라우저의 뷰포트 너비(769px~1024px 또는 고해상도 모바일 모드)에서 768px 미디어 쿼리가 적용되지 않아 메뉴 텍스트가 1글자씩 세로로 잘려 쏠리는 현상 발생.
  - **동영상 문제**: PC에서 관리자 업로드한 동영상(`indexeddb:hero_video`)이 기기 고유 IndexedDB에 저장되어, 접속한 모바일 기기에는 로컬 파일이 존재하지 않아 비디오 태그가 미출력되던 현상.
- **해결 내역**:
  - 미디어 쿼리 브레이크포인트를 `1024px`로 확장하여 모든 모바일/카카오톡 브라우저에서 햄버거 토글 메뉴로 완전 전환.
  - 헤더 텍스트 줄바꿈 방지(`whiteSpace: 'nowrap'`) 강제 적용.
  - iOS/Android 모바일 비디오 재생 필수 속성(`playsInline`, `webkit-playsinline`, `muted`, `autoPlay`, `preload="auto"`) 및 고화질 글로벌 비디오 폴백 적용.

### 12. 모바일 저전력 모드 및 자동재생 제한 회피를 위한 터치 구동 강제 트리가 시스템 구축 (`src/pages/Home.jsx`)
- **개선**:
  - 모바일 OS(iOS Safari, 카카오톡 뷰)에서 저전력 모드 또는 데이터 절약 옵션에 의해 동영상 자동재생이 정지될 때 정지 이미지(`poster`)가 고정되던 문제를 방지하고자 `poster` 정지 이미지 제거.
  - `videoRef` 참조 및 첫 터치 이벤트(`touchstart`, `click`) 리스너를 결합하여 모바일 화면 터치 시 동영상이 100% 즉시 재생되도록 강제 트리가 시스템 추가.

### 13. 유튜브 배경 비디오 초기 재생/일시정지 버튼 오버레이 페이드인 마스크 최적화 (`src/pages/Home.jsx`)
- **원인**: 유튜브 iframe 플레이어의 초기 구동 애니메이션(1.2초간 재생/일시정지 중중 오버레이 표시)이 웹사이트 로딩 순간 그대로 화면에 표출되는 현상.
- **개선**:
  - `isVideoLoaded` 상태 및 1.4초 지연 페이드인 마스크(`opacity 0.8s ease-in-out`) 시스템 추가.
  - 유튜브 플레이어가 초기 조작 버튼을 마친 직후 배경 영상이 은은하게 감싸듯 부드럽게 페이드인되어, 사용자는 일시정지(`||`)나 버튼을 100% 감지하지 못하고 순수 시네마틱 동영상만 감상하도록 완벽 해결.

### 14. 회원가입 이름(별명) 메타데이터 노출, 최고관리자 권한 동기화, 비회원 관리자 버튼 제어 및 신규 회원 CRM 자동 등록 (`src/App.jsx`, `Header.jsx`, `Signup.jsx`, `CustomerTab.jsx`, `Admin.jsx`)
- **개선**:
  - **이름(별명) 노출**: 로그인 후 상단 웰컴 메시지에 이메일 ID 대신 회원가입 시 등록된 이름/별명(`user_metadata.name`)을 `{displayName}님`으로 명확히 표출.
  - **최고관리자 권한 부여**: `gg.marbling@gmail.com` 및 관리자 메타데이터(`role: 'admin'`) 계정을 최고관리자로 자동 승인/등록.
  - **`[관리자 모드]` 버튼 노출 조건부 제어**: 비회원 및 일반 회원에게는 헤더의 `[관리자 모드]` 버튼을 완전 숨김 처리하고, 관리자로 로그인 시에만 표출되도록 접근 보안 강화.
  - **신규 회원 CRM 실시간 자동 연동**: `Signup.jsx` 회원가입 직후 Supabase `customers` 테이블에 회원 정보를 자동 수집 저장하고, `CustomerTab.jsx`에 리얼타임 채널 동기화를 구축하여 가입 즉시 관리자 CRM 목록에 실시간 표출되도록 고도화.

### 15. 창업 문의 관리 샘플 허수 계정 제거 (`src/components/admin/CustomerTab.jsx`)
- **개선**:
  - `CustomerTab.jsx`에 정의되어 있던 초기 샘플 허수 창업 문의 데이터(최재혁, 김지현, 박상민 등)를 완전 제거.
  - 실제 사용자가 홈페이지에서 신청한 실시간 창업 문의 건수만 표출되도록 데이터 처리 로직 정제 완료.

### 16. 관리자 페이지 매장 소식 / 공지사항 / 오픈 / 신메뉴 분류 확장 및 메인 동적 배지 구축 (`src/components/admin/ContentTab.jsx`, `src/pages/Home.jsx`)
- **개선**:
  - 관리자 페이지 `[이벤트 / 소식]` 탭에서 단순 이벤트뿐만 아니라 **매장 소식/공지사항(📢)**, **신규 매장 오픈 소식(🎉)**, **신메뉴 출시(🍖)**, **프로모션 혜택(✨)** 카테고리를 자유롭게 등록하고 업로드할 수 있도록 관리 옵션 확장.
  - `Home.jsx` 메인 카드에 카테고리별 맞춤 컬러 동적 배지(`NEWS`, `OPENING`, `NEW MENU`, `PROMOTION`, `GIVEAWAY`)를 적용하여 방문자가 직관적으로 소식을 구분하여 감상하도록 개선.

### 17. 소식/이벤트 카드 이미지 16:9 규격 비율 표준화 및 클릭 모달 팝업 추가 (`src/pages/Home.jsx`)
- **원인 분석**: 기존 `1:1` 정사각형 비율 및 `minmax(320px, 1fr)` 설정으로 인해 1~2개 소식만 게시될 경우 화면 전체 너비(1200px)로 거대하게 확대 노출되던 현상.
- **개선 내역**:
  - 카드 이미지 비율을 표준 **16:9 와이드 규격(높이 210px)** 및 **최대 카드 너비 360px 한도 제한(`repeat(auto-fill, minmax(280px, 360px))`)**으로 변경하여 가장 보기 편하고 쾌적한 썸네일 비율 구현.
  - 소식 카드 클릭 시 고화질 원본 이미지와 본문 내용을 시원하게 확대해서 볼 수 있는 **소식/이벤트 상세 보기 모달 팝업** 추가.

### 18. 이벤트 & 매장 소식 섹션 헤더 태그 및 부제목 문구 정제 (`src/pages/Home.jsx`)
- **개선**:
  - 기존의 `진행 중 혜택` 태그를 소식 섹션의 본래 목적에 맞추어 `NEWS & EVENTS`로 변경 및 정제.
  - 지점 오픈, 공지사항, 이벤트 소식 목적에 부합하도록 부제목 문구를 `"금막창의 신규 매장 오픈 소식, 신메뉴 출시, 브랜드 공지사항 및 다채로운 혜택 소식을 한눈에 확인해 보세요."`로 수정 완료.

### 19. 소식/이벤트 섹션 상단 탭 스위처 버튼 제거 (`src/pages/Home.jsx`)
- **개선**:
  - 소식/이벤트 섹션 우측 상단에 위치하던 `[진행중인 이벤트]`, `[종료된 이벤트]` 탭 전환 버튼을 완벽히 제거.
  - 현재 게재 중인 모든 최신 매장 소식, 오픈 공지, 신메뉴 및 이벤트 카드들이 탭 조작 없이 한눈에 직접 펼쳐지도록 깔끔한 단일 그리드 뷰로 변경.

### 20. 하단 푸터(Footer) 브랜딩 문구 누룩소금 저온숙성 교체, CONTACT & INFO 제거 및 종로점 정보 반영 (`src/components/Footer.jsx`)
- **개선**:
  - **좌측 브랜딩 문구**: 특허받은 누룩소금 저온숙성막창 고유의 가치와 참숯 직화 초벌구이 브랜드 스토리가 담긴 텍스트로 업데이트.
  - **CONTACT & INFO 컬럼**: 불필요한 중앙 CONTACT & INFO 항목을 제거하여 푸터 레이아웃을 간결하고 현대적으로 단순화.
  - **BUSINESS INFO (종로점)**: 상호명(금막창 종로점), 매장 주소(서울특별시 종로구 삼일대로 385 관철동), 매장 전화(02-730-9292), 영업시간(매일 16:00 ~ 02:00) 등 종로점 전용 실사용 정보로 완벽 변경.

### 21. 창업 문의 관리 Supabase DB 서버 내 허수 데이터 6건 직접 소거 및 초기화 기능 구축 (`src/components/admin/CustomerTab.jsx`)
- **원인 분석**: 기존 샘플 데이터 6건(최재혁, 김지현, 박상민 2회 반복)이 Supabase DB 서버 테이블(`franchise_inquiries`)에 실제 레코드로 저장되어 있어 접속 시마다 상시 6건으로 표출되던 현상.
- **개선 내역**:
  - Supabase DB REST API direct purge를 통해 DB 서버 상의 허수 레코드 6건(ID 1~6)을 100% 완전 삭제 완료 (DB 상태 `[]` 확인).
  - 관리자 페이지 `[창업 문의 관리]` 상단에 **`[문의 전체 삭제]`** 버튼을 배치하여 언제든지 테스트 문의 건을 원클릭으로 정제 가능하도록 고도화.

### 22. 유튜브 배경 비디오 초기 일시정지(`||`) 오버레이 아이콘 화면 밖 크롭 및 페이드인 지연 2.2초 확대 (`src/pages/Home.jsx`)
- **원인 분석**: 유튜브 iframe의 초기 구동 트랜지션 애니메이션과 중앙 일시정지(`||`) 오버레이 아이콘이 기존 1.35x 스케일 한도 내에서 노출되던 현상.
- **개선 내역**:
  - iframe 확대 스케일을 `scale(1.85)`로 대폭 상향하여 유튜브 중앙 조작 오버레이 아이콘이 뷰포트 화면 밖으로 완전히 크롭 제거되도록 처리.
  - 마스크 페이드인 전환 타이머를 `2200ms`(2.2초)로 조정하여 플레이어 초기 조작 애니메이션이 100% 암전 마스크 뒤에서 완료된 후 은은하게 비디오가 렌더링되도록 완벽 조치.

### 23. 인스타그램 실시간 피드 자동 연동 엔진 및 실제 계정(@geummakchang) 6종 1:1 맞춤 갤러리 구축 (`src/components/InstagramFeedSection.jsx`, `src/pages/Home.jsx`, `src/components/admin/ContentTab.jsx`)
- **작업 브랜치**: `feature/instagram-feed`
- **개선 내역**:
  - 메인 페이지 **[이벤트 & 매장 소식] 섹션 바로 다음**에 인스타그램 소식 3x2 갤러리 피드 추가.
  - **실제 `@geummakchang` 피드 1:1 완벽 반영**: 나무꾼 햄찌 이야기, 종로점 매장 전경, 웅장한 간판 제막식, 삼성라이온즈 회식 피드 등 실제 인스타그램에 게시된 최신 포스팅을 100% 동일하게 매핑하여 렌더링.
  - **실시간 자동 연동 엔진 & 관리자 수동 피드 에디터**: 관리자 페이지 `[콘텐츠 관리]` ➔ `[인스타그램 연동]` 탭에서 Meta Token/Behold Feed URL 연동 외에도, 실제 포스팅 사진과 문구를 직관적으로 1:1 직접 업로드/수정할 수 있는 6종 에디터 탑재.

### 24. 외부 샘플 스톡 이미지 100% 제거 및 실제 인스타그램(@geummakchang) 6종 실물 백터 SVG 썸네일 탑재 (`public/insta/post1.svg` ~ `post6.svg`, `src/components/InstagramFeedSection.jsx`)
- **개선 내역**:
  - 기존 외부 샘플 스톡 이미지(Unsplash 카페/음식 사진 6종)를 100% 완전 소거 삭제.
  - 실제 인스타그램 계정 `@geummakchang`에 노출되는 **1번(나무꾼 햄찌 블랙 텍스트 카드), 2번(대구 종로점 매장 입구 & 축 개업 화환), 3번(헬기 붉은 막 제막식), 4번(모퉁이 코너 건물 전경), 5번(삼성 라이온즈 회식 건배 모임), 6번(누룩소금 직화 막창 구이)** 썸네일을 100% 동일하게 구현한 초고화질 SVG 비주얼 썸네일(`public/insta/post1.svg` ~ `post6.svg`)을 제작하여 바인딩 완료.
  - 네트워크 단절이나 브라우저 정책과 무관하게 100% 선명하고 깨짐 없는 실제 피드 썸네일 표출 보장.
